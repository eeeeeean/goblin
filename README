Goblin is a simple irc bot. It works like this:

require 'goblin'
a = ["Some", "error", "messages"]
Goblin.new(a, recipient_irc_nick, server, port).run

Goblin will log into irc, send you a pm of error messages, quit.
